spring.application.name=plugin-template

# Core URL (modify this to your core system's actual URL)
core.url=http://localhost:8080/api/plugins/heartbeat

# Plugin ID (unique for each plugin)
plugin.id=plugin-1
